import VoteCounter from '../VoteCounter/VoteCounter';
import './MainContent.scss';

const MainContent = () => {

    return (
        <main className="main">
            <VoteCounter />
        </main>
    );
}

export default MainContent;
